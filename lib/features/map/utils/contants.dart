import 'package:google_maps_flutter/google_maps_flutter.dart';

/// The default camera position for the map, centered on Adelaide, Australia.
const kDefaultCameraPosition = CameraPosition(
  target: LatLng(-34.9285, 138.6007),
  zoom: 12.0,
);
